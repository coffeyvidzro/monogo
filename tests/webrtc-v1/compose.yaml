services:
  server:
    ports:
      - "127.0.0.1:8080:8080"
  rtpengine:
    # Override the base Compose PUBLIC_IP (127.0.0.1 in this runner).
    # The browser reaches RTPengine on the Docker media bridge, not loopback.
    environment:
      RTPENGINE_PUBLIC_IP: ${RTPENGINE_PUBLIC_IP:?RTPENGINE_PUBLIC_IP is required}
    ports: !reset []

  coturn:
    # Fixed test-network relay address shared with RTPengine and the Linux runner.
    networks:
      media:
        ipv4_address: 172.31.0.30
    environment:
      TURN_REALM: ${TURN_REALM:?TURN_REALM is required}
      TURN_AUTH_SECRET: ${TURN_AUTH_SECRET:?TURN_AUTH_SECRET is required}
      TURN_EXTERNAL_IP: ${TURN_EXTERNAL_IP:?TURN_EXTERNAL_IP is required}
      TURN_MIN_PORT: "${WEBRTC_V1_TURN_MIN_PORT:?WEBRTC_V1_TURN_MIN_PORT is required}"
      TURN_MAX_PORT: "${WEBRTC_V1_TURN_MAX_PORT:?WEBRTC_V1_TURN_MAX_PORT is required}"
    volumes:
      - ${WEBRTC_V1_CERT_DIR:?WEBRTC_V1_CERT_DIR is required}/opensips-fullchain.pem:/etc/coturn/tls/fullchain.pem:ro
      - ${WEBRTC_V1_CERT_DIR:?WEBRTC_V1_CERT_DIR is required}/opensips-privkey.pem:/etc/coturn/tls/privkey.pem:ro
    ports: !override
      - "3478:3478/udp"
      - "3478:3478/tcp"
      - "${WEBRTC_V1_TURN_MIN_PORT:?WEBRTC_V1_TURN_MIN_PORT is required}-${WEBRTC_V1_TURN_MAX_PORT:?WEBRTC_V1_TURN_MAX_PORT is required}:${WEBRTC_V1_TURN_MIN_PORT:?WEBRTC_V1_TURN_MIN_PORT is required}-${WEBRTC_V1_TURN_MAX_PORT:?WEBRTC_V1_TURN_MAX_PORT is required}/udp"

  opensips:
    environment:
      OPENSIPS_ADVERTISED_ADDRESS: 127.0.0.1
    volumes:
      - ${WEBRTC_V1_CERT_DIR:?WEBRTC_V1_CERT_DIR is required}/opensips-fullchain.pem:/etc/opensips/tls/fullchain.pem:ro
      - ${WEBRTC_V1_CERT_DIR:?WEBRTC_V1_CERT_DIR is required}/opensips-privkey.pem:/etc/opensips/tls/privkey.pem:ro
      - ${WEBRTC_V1_CERT_DIR:?WEBRTC_V1_CERT_DIR is required}/opensips-carrier-ca.pem:/etc/opensips/tls/carrier-ca.pem:ro
