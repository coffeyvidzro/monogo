name: BYOC acceptance

on:
  push:
    branches:
      - main
    paths:
      - ".github/workflows/byoc-acceptance.yml"
      - ".env.example"
      - "containers/**"
      - "deploy/**"
      - "server/**"
      - "tests/byoc-v1/**"
  pull_request:
    branches:
      - main
    paths:
      - ".github/workflows/byoc-acceptance.yml"
      - ".env.example"
      - "containers/**"
      - "deploy/**"
      - "server/**"
      - "tests/byoc-v1/**"
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: byoc-acceptance-${{ github.ref }}
  cancel-in-progress: true

jobs:
  acceptance:
    name: BYOC acceptance
    runs-on: ubuntu-latest
    timeout-minutes: 45

    steps:
      - name: Checkout
        uses: actions/checkout@v7.0.1

      - name: Run byoc acceptance
        run: sh tests/byoc-v1/run.sh
